#include <iostream>
using namespace std;

int main(){

    int x = 5;
    int y = 6;
    cout << (x>y?x:y) << endl;

    //ternary operator follows if else logic
    // its basically (if condition statement ? then block ? else block)
    
}